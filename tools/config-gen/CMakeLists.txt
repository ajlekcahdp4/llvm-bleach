install(
  FILES config-gen.rb
  DESTINATION ${CMAKE_INSTALL_BINDIR}
  PERMISSIONS
    OWNER_READ
    OWNER_WRITE
    OWNER_EXECUTE
    GROUP_READ
    GROUP_EXECUTE
    WORLD_READ
    WORLD_EXECUTE
  RENAME bleach-config-gen)

install(DIRECTORY templates DESTINATION ${CMAKE_INSTALL_DATADIR})
